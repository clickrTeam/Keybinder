#include "event.h"

void mapper_test_timed(Profile p, QList<KeyEvent> events, QList<uint64_t> waits,
                       QList<OutputEvent> outputs);

void mapper_test(Profile p, QList<KeyEvent> events, QList<OutputEvent> outputs);
